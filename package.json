{
  "name": "obsidian-gym-tracker",
  "version": "0.1.0",
  "description": "Obsidian plugin for tracking gym workouts with progressive overload focus",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "npm run lint && npm run typecheck && node esbuild.config.mjs production",
    "typecheck": "tsc -noEmit",
    "lint": "oxlint --deny-warnings -c oxlint.json src/ test/",
    "lint:fix": "oxlint --fix -c oxlint.json src/ test/",
    "format": "oxfmt --write src/ test/",
    "format:check": "oxfmt --check src/ test/",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run lint && npm run typecheck && npm run test",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "gym",
    "workout",
    "fitness",
    "tracker"
  ],
  "author": "gricha",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.19.30",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "oxfmt": "^0.26.0",
    "oxlint": "^1.41.0",
    "tslib": "^2.6.2",
    "typescript": "^5.3.3",
    "vitest": "^4.0.17"
  }
}
